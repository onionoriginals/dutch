# Development Environment Variables

# API Configuration
PORT=3000
HOST=::
BITCOIN_NETWORK=testnet

# Encryption (required for production)
AUCTION_ENCRYPTION_PASSWORD=your-secure-password-here

# CORS (optional, defaults provided in code)
# ALLOWED_ORIGINS=http://localhost:4321,https://your-domain.com

# Logging
LOG_LEVEL=info
LOG_FORMAT=text

# Web Development (for separate dev server)
# When running web dev server separately, point to API
# PUBLIC_API_BASE=http://localhost:3000
